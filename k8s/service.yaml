apiVersion: v1
kind: Service
metadata:
  name: go-otel-service
  namespace: default
  labels:
    app: go-otel
spec:
  type: ClusterIP
  selector:
    app: go-otel
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  sessionAffinity: None
